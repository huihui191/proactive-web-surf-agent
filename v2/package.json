{
  "name": "proactive-web-surf-agent-v2",
  "version": "0.2.0-beta.1",
  "private": true,
  "description": "A ready-to-run proactive web surf companion for API users.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/*.test.js",
    "check": "npm test",
    "once": "npm run build && node dist/index.js --once",
    "start": "node dist/index.js"
  },
  "engines": { "node": ">=20" },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "typescript": "^5.9.2"
  }
}
